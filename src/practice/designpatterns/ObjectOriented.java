package practice.designpatterns;

/**
 * 
 * @author Prachi
 * 
 */
public class ObjectOriented {

	/**
	 * Amazon on-site ProofPoint on-site
	 */
	// Design a parking lot

	/**
	 * Amazon on-site What are the design patterns are you aware of. Name a few.
	 * My Answer 1. Singleton, Factory(Used this pattern in my file-system
	 * solution), Abstract Factory, Builder, Facade(actually used this pattern
	 * while implementing the file system solution. Design a file-system),
	 * Observer pattern.
	 */
}
